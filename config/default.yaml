# Strategy parameters (universe, dual-momentum thresholds, orchestrator
# toggles) live in src/aurel2/config/canonical.py, NOT here — that module is
# the single config artifact both the live checker and BacktestEngine load,
# per docs/plans/2026-07-09-edge-decomposition-goal.md Phase 2 non-negotiable #1.
# This file only holds settings that aren't part of the trading decision path.

broker:
  type: alpaca
  paper: true

risk:
  max_position_pct: 1.0
  transaction_cost_pct: 0.001

logging:
  level: INFO
